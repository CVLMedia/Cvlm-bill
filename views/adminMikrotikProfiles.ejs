<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Profile PPPoE</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/responsive/2.5.0/css/responsive.bootstrap5.min.css" rel="stylesheet">
    <link href="/css/responsive-admin.css" rel="stylesheet">
    <style>
        body { background: #f5f6fa; }
        .form-label { font-weight: 600; }

        .main-content {
            padding: 32px 24px;
        }
        
        /* Modern Notification Modal - Neumorphic Style */
        .notification-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none; /* default: tidak tampil dan tidak blok UI */
            justify-content: center;
            align-items: center;
            z-index: 10000;
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none; /* jangan blok klik ketika hidden */
        }
        
        .notification-overlay.show {
            display: flex;       /* hanya tampil ketika ada class show */
            opacity: 1;
            pointer-events: auto;
        }
        
        .notification-card {
            background: linear-gradient(145deg, #ffffff, #f0f0f0);
            border-radius: 24px;
            padding: 40px;
            max-width: 400px;
            width: 90%;
            box-shadow: 
                20px 20px 60px #d0d0d0,
                -20px -20px 60px #ffffff,
                inset 0 0 0 rgba(255, 255, 255, 0),
                inset 0 0 0 rgba(0, 0, 0, 0);
            text-align: center;
            transform: scale(0.8);
            transition: transform 0.3s ease;
            position: relative;
        }
        
        .notification-overlay.show .notification-card {
            transform: scale(1);
        }
        
        .notification-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
            color: white;
            position: relative;
            box-shadow: 
                8px 8px 16px rgba(0, 0, 0, 0.2),
                -8px -8px 16px rgba(255, 255, 255, 0.3);
        }
        
        .notification-icon.success {
            background: linear-gradient(145deg, #4CAF50, #45a049);
        }
        
        .notification-icon.error {
            background: linear-gradient(145deg, #f44336, #d32f2f);
        }
        
        .notification-icon.warning {
            background: linear-gradient(145deg, #ff9800, #f57c00);
        }
        
        .notification-icon.info {
            background: linear-gradient(145deg, #2196F3, #1976D2);
        }
        
        .notification-title {
            font-size: 28px;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 12px;
        }
        
        .notification-message {
            font-size: 16px;
            color: #5a6c7d;
            margin-bottom: 30px;
            line-height: 1.5;
        }
        
        .notification-button {
            background: linear-gradient(145deg, #2196F3, #1976D2);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 14px 32px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 
                6px 6px 12px rgba(0, 0, 0, 0.15),
                -6px -6px 12px rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            min-width: 120px;
        }
        
        .notification-button:hover {
            transform: translateY(-2px);
            box-shadow: 
                8px 8px 16px rgba(0, 0, 0, 0.2),
                -8px -8px 16px rgba(255, 255, 255, 0.15);
        }
        
        .notification-button:active {
            transform: translateY(0);
            box-shadow: 
                4px 4px 8px rgba(0, 0, 0, 0.1),
                -4px -4px 8px rgba(255, 255, 255, 0.1);
        }
        
        .notification-button.success {
            background: linear-gradient(145deg, #4CAF50, #45a049);
        }
        
        .notification-button.error {
            background: linear-gradient(145deg, #f44336, #d32f2f);
        }
        
        .notification-button.warning {
            background: linear-gradient(145deg, #ff9800, #f57c00);
        }
        
        .notification-button-secondary {
            background: linear-gradient(145deg, #6c757d, #5a6268) !important;
        }
        
        #notificationButtons {
            display: flex;
            justify-content: center;
            gap: 10px;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        }
        /* Modern Metric Cards Styling - Same as admin/dashboard */
        .metric-card {
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1), 0 2px 4px rgba(0, 0, 0, 0.06);
            transition: all 0.3s ease;
            border: none;
            position: relative;
            overflow: hidden;
            background: var(--card-bg-color);
            color: #ffffff;
            min-height: auto;
        }
        
        .metric-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.2), 0 4px 6px rgba(0, 0, 0, 0.15);
        }
        
        .metric-card-icon {
            width: 48px;
            height: 48px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 12px;
            background: rgba(255, 255, 255, 0.2);
            color: #ffffff;
            font-size: 24px;
            backdrop-filter: blur(10px);
        }
        
        .metric-card-value {
            font-size: 1.75rem;
            font-weight: 700;
            color: #ffffff;
            margin: 8px 0;
            line-height: 1.2;
        }
        
        .metric-card-label {
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
            margin-top: 4px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        /* Card Colors - Gradient Backgrounds */
        .metric-card-primary {
            --card-bg-color: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
        }
        
        .metric-card-success {
            --card-bg-color: linear-gradient(135deg, #10b981 0%, #34d399 100%);
            background: linear-gradient(135deg, #10b981 0%, #34d399 100%);
        }
        
        .metric-card-info {
            --card-bg-color: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
            background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
        }
        
        .metric-card-warning {
            --card-bg-color: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);
            background: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .metric-card {
                padding: 16px;
            }
            
            .metric-card-value {
                font-size: 1.5rem;
            }
            
            .metric-card-icon {
                width: 40px;
                height: 40px;
                font-size: 20px;
            }
        }
        .dataTables_wrapper .dataTables_filter input {
            border-radius: 6px;
            border: 1px solid #ccc;
            padding: 4px 10px;
        }
        .table th {
            background-color: #f8f9fa;
            font-weight: 600;
            font-size: 0.9rem;
        }
        .table td {
            vertical-align: middle;
            font-size: 0.9rem;
        }
        .badge {
            font-size: 0.75rem;
        }
        code {
            background-color: #f8f9fa;
            padding: 2px 4px;
            border-radius: 3px;
            font-size: 0.85rem;
        }
        .btn-sm {
            padding: 0.25rem 0.5rem;
            font-size: 0.8rem;
        }
        .table-responsive {
            border-radius: 8px;
            overflow: hidden;
        }
        .alert-info {
            background-color: #e7f3ff;
            border-color: #b3d9ff;
            color: #0c5460;
        }
        .spin {
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        pre {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
        }
        .table-sm td, .table-sm th {
            padding: 0.3rem;
            font-size: 0.85rem;
        }
        
        /* Mobile responsive improvements */
        @media (max-width: 768px) {
            .table-responsive {
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
            }
            
            .table th, .table td {
                white-space: nowrap;
                min-width: 120px;
            }
            
            .table th:first-child, .table td:first-child {
                min-width: 50px;
            }
            
            .btn-sm {
                padding: 0.2rem 0.4rem;
                font-size: 0.75rem;
            }
            
            .card-title {
                font-size: 1.5rem;
            }
            
            .main-content {
                padding: 16px 12px;
            }
        }
        @media (max-width: 991.98px) {
            .sidebar {
                position: fixed;
                left: -260px;
                top: 0;
                width: 260px;
                height: 100vh;
                z-index: 1050;
                transition: left 0.3s;
            }
            .sidebar.show {
                left: 0;
            }
            .sidebar-overlay {
                display: none;
                position: fixed;
                top: 0; left: 0; right: 0; bottom: 0;
                background: rgba(0,0,0,0.3);
                z-index: 1049;
            }
            .sidebar-overlay.show {
                display: block;
            }
            .main-content {
                padding-left: 0 !important;
            }
            .row.mb-4 > [class^='col-'] {
                margin-bottom: 1rem !important;
            }
        }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <!-- Include Responsive Admin Sidebar -->
        <%- include('partials/admin-responsive-sidebar', { page: 'mikrotik-profiles', settings: settings }) %>
        
        <main class="col-md-10 main-content ms-sm-auto">
            <button class="hamburger d-block d-md-none mb-3" id="hamburgerBtn" aria-label="Menu"><i class="bi bi-list"></i></button>

            <h3 class="mb-4">Daftar Profile PPPoE Mikrotik</h3>
            
            <!-- Statistik Profile -->
            <div class="row mb-4">
                <div class="col-md-3 mb-3">
                    <div class="metric-card metric-card-primary">
                        <div class="metric-card-icon">
                            <i class="bi bi-list-ul"></i>
                        </div>
                        <div class="metric-card-value"><%= profiles.length %></div>
                        <div class="metric-card-label">Total Profile</div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="metric-card metric-card-success">
                        <div class="metric-card-icon">
                            <i class="bi bi-star"></i>
                        </div>
                        <div class="metric-card-value"><%= profiles.filter(p => p.default === 'true').length %></div>
                        <div class="metric-card-label">Profile Default</div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="metric-card metric-card-info">
                        <div class="metric-card-icon">
                            <i class="bi bi-speedometer2"></i>
                        </div>
                        <div class="metric-card-value"><%= profiles.filter(p => p['rate-limit']).length %></div>
                        <div class="metric-card-label">Dengan Rate Limit</div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="metric-card metric-card-warning">
                        <div class="metric-card-icon">
                            <i class="bi bi-chat-left-text"></i>
                        </div>
                        <div class="metric-card-value"><%= profiles.filter(p => p.comment).length %></div>
                        <div class="metric-card-label">Dengan Komentar</div>
                    </div>
                </div>
            </div>
            
            <button class="btn btn-success mb-3" id="btnTambahProfile">Tambah Profile PPPoE</button>
            <button class="btn btn-outline-primary mb-3 ms-2" id="btnRefresh">
                <i class="bi bi-arrow-clockwise"></i> Refresh Data
            </button>
            
            <!-- Filter dan Pencarian -->
            <div class="row mb-3">
                <div class="col-md-6">
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-search"></i></span>
                        <input type="text" class="form-control" id="searchInput" placeholder="Cari profile...">
                    </div>
                </div>
                <div class="col-md-3">
                    <select class="form-select" id="statusFilter">
                        <option value="">Semua Status</option>
                        <option value="default">Default</option>
                        <option value="custom">Custom</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <select class="form-select" id="rateFilter">
                        <option value="">Semua Rate</option>
                        <option value="has-rate">Dengan Rate Limit</option>
                        <option value="no-rate">Tanpa Rate Limit</option>
                    </select>
                </div>
            </div>

            <div class="table-responsive">
                <table class="table table-bordered table-hover" id="profileTable" style="width:100%">
                    <thead class="table-light">
                        <tr>
                            <th>No</th>
                            <th>Nama Profile</th>
                            <th>NAS</th>
                            <th>Rate Limit</th>
                            <th>Local Address</th>
                            <th>Remote Address</th>
                            <th>DNS Server</th>
                            <th>Status</th>
                            <th>Komentar</th>
                            <th>Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% profiles.forEach(function(profile, i) { %>
                        <tr>
                            <td><%= i+1 %></td>
                            <td>
                                <strong><%= profile.name %></strong>
                                <% if(profile.default === 'true') { %>
                                    <span class="badge bg-primary ms-1">Default</span>
                                <% } %>
                            </td>
                            <td>
                                <% if (profile.nas_name) { %>
                                    <span class="badge bg-secondary"><%= profile.nas_name %></span>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile['rate-limit']) { %>
                                    <code class="text-success"><%= profile['rate-limit'] %></code>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile['local-address']) { %>
                                    <code><%= profile['local-address'] %></code>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile['remote-address']) { %>
                                    <code><%= profile['remote-address'] %></code>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile['dns-server']) { %>
                                    <code><%= profile['dns-server'] %></code>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile.default === 'true') { %>
                                    <span class="badge bg-success">Default</span>
                                <% } else { %>
                                    <span class="badge bg-secondary">Custom</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(profile.comment) { %>
                                    <small class="text-muted"><%= profile.comment %></small>
                                <% } else { %>
                                    <span class="text-muted">-</span>
                                <% } %>
                            </td>
                            <td>
                                <button class="btn btn-sm btn-info view-profile-btn" 
                                        data-id="<%= profile['.id'] %>" 
                                        data-name="<%= profile.name %>"
                                        title="Lihat Detail">
                                    <i class="bi bi-eye"></i>
                                </button>
                                <% if(profile.default !== 'true') { %>
                                <button class="btn btn-sm btn-warning edit-profile-btn" 
                                        data-id="<%= profile['.id'] %>" 
                                        data-name="<%= profile.name %>" 
                                        data-router-id="<%= profile.nas_id || '' %>"
                                        data-rate-limit="<%= profile['rate-limit'] || '' %>"
                                        data-local-address="<%= profile['local-address'] || '' %>"
                                        data-remote-address="<%= profile['remote-address'] || '' %>"
                                        data-dns-server="<%= profile['dns-server'] || '' %>"
                                        data-parent-queue="<%= profile['parent-queue'] || '' %>"
                                        data-address-list="<%= profile['address-list'] || '' %>"
                                        data-comment="<%= profile.comment || '' %>"
                                        data-bridge-learning="<%= profile['bridge-learning'] || 'default' %>"
                                        data-use-mpls="<%= profile['use-mpls'] || 'default' %>"
                                        data-use-compression="<%= profile['use-compression'] || 'default' %>"
                                        data-use-encryption="<%= profile['use-encryption'] || 'default' %>"
                                        data-only-one="<%= profile['only-one'] || 'default' %>"
                                        data-change-tcp-mss="<%= profile['change-tcp-mss'] || 'default' %>"
                                        title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button class="btn btn-sm btn-danger delete-profile-btn" 
                                        data-id="<%= profile['.id'] %>" 
                                        data-name="<%= profile.name %>"
                                        data-router-id="<%= profile.nas_id || '' %>"
                                        title="Hapus">
                                    <i class="bi bi-trash"></i>
                                </button>
                                <% } %>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>

<!-- Modal Tambah/Edit Profile PPPoE -->
<div class="modal fade" id="profileModal" tabindex="-1" aria-labelledby="profileModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <form id="profileForm">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="profileModalLabel">Tambah Profile PPPoE</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="profileId" name="id">
                    
                    <div class="mb-3">
                        <label for="routerId" class="form-label"><i class="bi bi-router me-1"></i>NAS (Router) *</label>
                        <select class="form-select" id="routerId" name="router_id" required>
                            <% if (routers && routers.length > 0) { %>
                                <option value="">Pilih NAS</option>
                                <% routers.forEach(function(router) { %>
                                    <option value="<%= router.id %>"><%= router.name %> (<%= router.nas_ip %>)</option>
                                <% }); %>
                            <% } else { %>
                                <option value="">Tidak ada router tersedia</option>
                            <% } %>
                        </select>
                        <small class="text-muted">Pilih NAS (router) tempat profile akan dibuat</small>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="profileName" class="form-label">Nama Profile *</label>
                                <input type="text" class="form-control" id="profileName" name="name" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="rateLimit" class="form-label">Rate Limit</label>
                                <div class="input-group">
                                    <input type="text" class="form-control" id="rateLimit" name="rate-limit" 
                                           placeholder="Contoh: 1M/1M atau 756K/756K 1M/1M 384K/384K">
                                    <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                        Preset
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('1M/1M')">1 Mbps</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('2M/2M')">2 Mbps</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('5M/5M')">5 Mbps</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('10M/10M')">10 Mbps</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('756K/756K 1M/1M 384K/384K')">1M (Queue)</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('2M/2M 5M/5M 1500K/1500K')">2M (Queue)</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('3M/3M 5M/5M 2250K/2250K')">3M (Queue)</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setRateLimit('5M/5M 10M/10M 3750K/3750K')">5M (Queue)</a></li>
                                    </ul>
                                </div>
                                <small class="text-muted">Format: upload/download atau upload/download max-limit burst-limit</small>
                            </div>
                            
                            <div class="mb-3">
                                <label for="localAddress" class="form-label">Local Address</label>
                                <div class="input-group">
                                    <input type="text" class="form-control" id="localAddress" name="local-address" 
                                           placeholder="Contoh: 192.168.10.1">
                                    <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                        Preset
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="#" onclick="setLocalAddress('192.168.10.1')">192.168.10.1</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setLocalAddress('192.168.1.1')">192.168.1.1</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setLocalAddress('192.168.2.1')">192.168.2.1</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setLocalAddress('192.168.205.1')">192.168.205.1</a></li>
                                    </ul>
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <label for="remoteAddress" class="form-label">Remote Address (IP POOL)</label>
                                <select class="form-select" id="remoteAddress" name="remote-address">
                                    <option value="">Pilih IP Pool</option>
                                </select>
                                <small class="text-muted">IP Pool akan otomatis di-sync dari semua router MikroTik</small>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="dnsServer" class="form-label">DNS Server</label>
                                <div class="input-group">
                                    <input type="text" class="form-control" id="dnsServer" name="dns-server" 
                                           placeholder="Contoh: 8.8.8.8,8.8.4.4">
                                    <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                        Preset
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="#" onclick="setDnsServer('8.8.8.8,8.8.4.4')">Google DNS</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setDnsServer('1.1.1.1,1.0.0.1')">Cloudflare DNS</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setDnsServer('208.67.222.222,208.67.220.220')">OpenDNS</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" href="#" onclick="setDnsServer('192.168.10.1')">Local Router</a></li>
                                        <li><a class="dropdown-item" href="#" onclick="setDnsServer('8.8.8.8')">Google DNS (Primary)</a></li>
                                    </ul>
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <label for="parentQueue" class="form-label">Parent Queue</label>
                                <input type="text" class="form-control" id="parentQueue" name="parent-queue" 
                                       placeholder="Contoh: none atau nama queue">
                            </div>
                            
                            <div class="mb-3">
                                <label for="addressList" class="form-label">Address List</label>
                                <input type="text" class="form-control" id="addressList" name="address-list" 
                                       placeholder="Contoh: nama address list">
                            </div>
                            
                            <div class="mb-3">
                                <label for="profileComment" class="form-label">Komentar</label>
                                <textarea class="form-control" id="profileComment" name="comment" rows="3" 
                                          placeholder="Komentar opsional untuk profile ini"></textarea>
                            </div>
                        </div>
                    </div>
                    
                    <hr>
                    <h6 class="text-info">Konfigurasi Lanjutan</h6>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="bridgeLearning" class="form-label">Bridge Learning</label>
                                <select class="form-select" id="bridgeLearning" name="bridge-learning">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="useMpls" class="form-label">Use MPLS</label>
                                <select class="form-select" id="useMpls" name="use-mpls">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="useCompression" class="form-label">Use Compression</label>
                                <select class="form-select" id="useCompression" name="use-compression">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="useEncryption" class="form-label">Use Encryption</label>
                                <select class="form-select" id="useEncryption" name="use-encryption">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="onlyOne" class="form-label">Only One</label>
                                <select class="form-select" id="onlyOne" name="only-one">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="changeTcpMss" class="form-label">Change TCP MSS</label>
                                <select class="form-select" id="changeTcpMss" name="change-tcp-mss">
                                    <option value="default">Default</option>
                                    <option value="yes">Yes</option>
                                    <option value="no">No</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
                    <button type="submit" class="btn btn-primary">Simpan</button>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Modal Detail Profile PPPoE -->
<div class="modal fade" id="detailModal" tabindex="-1" aria-labelledby="detailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="detailModalLabel">Detail Profile PPPoE</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div id="profileDetailContent">
                    <!-- Content will be loaded here -->
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Tutup</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal Konfirmasi Restart Mikrotik -->
<div class="modal fade" id="restartMikrotikModal" tabindex="-1" aria-labelledby="restartMikrotikModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="restartMikrotikModalLabel"><i class="bi bi-arrow-repeat"></i> Konfirmasi Restart Mikrotik</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Apakah Anda yakin ingin <b>restart Mikrotik</b>?<br>Router akan reboot dan koneksi internet pelanggan akan terputus sementara.
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
        <button type="button" class="btn btn-danger" id="confirmRestartMikrotik">Restart</button>
      </div>
    </div>
  </div>
</div>
<!-- Notifikasi -->
<div id="restartNotif" class="alert d-none position-fixed top-0 end-0 m-4" style="z-index:9999; min-width:300px;"></div>

<!-- Modern Notification Modal -->
<div id="notificationModal" class="notification-overlay">
    <div class="notification-card">
        <div class="notification-icon" id="notificationIcon">
            <i class="bi" id="notificationIconClass"></i>
        </div>
        <div class="notification-title" id="notificationTitle"></div>
        <div class="notification-message" id="notificationMessage"></div>
        <div id="notificationButtons">
            <button class="notification-button" id="notificationButton" onclick="closeNotification()"></button>
            <button class="notification-button notification-button-secondary" id="notificationButtonCancel" onclick="closeNotification()" style="display: none; margin-left: 10px; background: linear-gradient(145deg, #6c757d, #5a6268);"></button>
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.5.0/js/dataTables.responsive.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.5.0/js/responsive.bootstrap5.min.js"></script>

<script>
// Modern Notification Function
function showNotification(type, title, message, buttonText, callback) {
    const modal = document.getElementById('notificationModal');
    const icon = document.getElementById('notificationIcon');
    const iconClass = document.getElementById('notificationIconClass');
    const titleEl = document.getElementById('notificationTitle');
    const messageEl = document.getElementById('notificationMessage');
    const button = document.getElementById('notificationButton');
    const buttonCancel = document.getElementById('notificationButtonCancel');
    
    // Hide cancel button for simple notifications
    buttonCancel.style.display = 'none';
    
    // Set icon
    icon.className = 'notification-icon ' + type;
    if (type === 'success') {
        iconClass.className = 'bi bi-check-circle-fill';
    } else if (type === 'error') {
        iconClass.className = 'bi bi-x-circle-fill';
    } else if (type === 'warning') {
        iconClass.className = 'bi bi-exclamation-triangle-fill';
    } else {
        iconClass.className = 'bi bi-info-circle-fill';
    }
    
    // Set content
    titleEl.textContent = title;
    messageEl.textContent = message;
    button.textContent = buttonText;
    button.className = 'notification-button ' + type;
    
    // Show modal
    modal.classList.add('show');
    
    // Set button click handler
    button.onclick = function() {
        closeNotification();
        if (callback && typeof callback === 'function') {
            callback();
        }
    };
    
    // Auto close after 5 seconds if no callback
    if (!callback) {
        setTimeout(function() {
            closeNotification();
        }, 5000);
    }
}

// Confirm Notification Function (with 2 buttons)
function showConfirmNotification(type, title, message, confirmText, cancelText, confirmCallback) {
    const modal = document.getElementById('notificationModal');
    const icon = document.getElementById('notificationIcon');
    const iconClass = document.getElementById('notificationIconClass');
    const titleEl = document.getElementById('notificationTitle');
    const messageEl = document.getElementById('notificationMessage');
    const button = document.getElementById('notificationButton');
    const buttonCancel = document.getElementById('notificationButtonCancel');
    
    // Show both buttons for confirm
    buttonCancel.style.display = 'inline-block';
    
    // Set icon
    icon.className = 'notification-icon ' + type;
    if (type === 'success') {
        iconClass.className = 'bi bi-check-circle-fill';
    } else if (type === 'error') {
        iconClass.className = 'bi bi-x-circle-fill';
    } else if (type === 'warning') {
        iconClass.className = 'bi bi-exclamation-triangle-fill';
    } else {
        iconClass.className = 'bi bi-info-circle-fill';
    }
    
    // Set content
    titleEl.textContent = title;
    messageEl.textContent = message;
    button.textContent = confirmText;
    button.className = 'notification-button ' + type;
    buttonCancel.textContent = cancelText;
    
    // Show modal
    modal.classList.add('show');
    
    // Set button click handlers
    button.onclick = function() {
        closeNotification();
        if (confirmCallback && typeof confirmCallback === 'function') {
            confirmCallback();
        }
    };
    
    buttonCancel.onclick = function() {
        closeNotification();
    };
}

function closeNotification() {
    const modal = document.getElementById('notificationModal');
    modal.classList.remove('show');
}

// Close on overlay click
document.addEventListener('DOMContentLoaded', function() {
    const modal = document.getElementById('notificationModal');
    if (modal) {
        modal.addEventListener('click', function(e) {
            if (e.target === this) {
                closeNotification();
            }
        });
    }
});

$(function() {
    let profileTable = new DataTable('#profileTable', {
        "pageLength": 25,
        "order": [[1, "asc"]],
        "responsive": true,
        "scrollX": true,
        "columnDefs": [
            {
                "targets": [0], // No column
                "responsivePriority": 1
            },
            {
                "targets": [1], // Nama Profile column
                "responsivePriority": 2
            },
            {
                "targets": [-1], // Aksi column (last column)
                "responsivePriority": 3,
                "orderable": false
            }
        ]
    });

    // Fungsi untuk load IP Pools dari semua router
    function loadIPPools(callback) {
        const remoteAddressSelect = $('#remoteAddress');
        const currentValue = remoteAddressSelect.val(); // Simpan nilai saat ini jika ada
        remoteAddressSelect.html('<option value="">Memuat IP Pool...</option>');
        remoteAddressSelect.prop('disabled', true);
        
        $.get('/admin/mikrotik/ip-pools', function(res) {
            remoteAddressSelect.prop('disabled', false);
            if (res.success && res.data && res.data.length > 0) {
                remoteAddressSelect.html('<option value="">Pilih IP Pool</option>');
                res.data.forEach(function(pool) {
                    const displayText = pool.ranges ? `${pool.name} (${pool.ranges})` : pool.name;
                    remoteAddressSelect.append(`<option value="${pool.name}">${displayText}</option>`);
                });
                // Set nilai sebelumnya jika ada
                if (currentValue) {
                    remoteAddressSelect.val(currentValue);
                }
            } else {
                remoteAddressSelect.html('<option value="">Tidak ada IP Pool ditemukan</option>');
            }
            // Panggil callback jika ada
            if (callback && typeof callback === 'function') {
                callback();
            }
        }).fail(function() {
            remoteAddressSelect.prop('disabled', false);
            remoteAddressSelect.html('<option value="">Gagal memuat IP Pool</option>');
            if (callback && typeof callback === 'function') {
                callback();
            }
        });
    }

    // Tambah Profile
    $('#btnTambahProfile').on('click', function() {
        $('#profileId').val('');
        $('#routerId').val('');
        $('#profileName').val('');
        $('#rateLimit').val('');
        $('#localAddress').val('');
        $('#remoteAddress').val('');
        $('#dnsServer').val('');
        $('#parentQueue').val('');
        $('#addressList').val('');
        $('#profileComment').val('');
        $('#bridgeLearning').val('default');
        $('#useMpls').val('default');
        $('#useCompression').val('default');
        $('#useEncryption').val('default');
        $('#onlyOne').val('default');
        $('#changeTcpMss').val('default');
        $('#profileModalLabel').text('Tambah Profile PPPoE');
        loadIPPools(); // Load IP pools saat modal dibuka
        $('#profileModal').modal('show');
    });

    // View Detail Profile
    $(document).on('click', '.view-profile-btn', function() {
        const profileId = $(this).data('id');
        const profileName = $(this).data('name');
        
        // Tampilkan loading
        $('#detailModalLabel').text(`Loading...`);
        $('#profileDetailContent').html('<div class="text-center"><i class="bi bi-arrow-clockwise spin"></i> Loading...</div>');
        $('#detailModal').modal('show');
        
        // Ambil detail profile dari API
        $.get(`/admin/mikrotik/profile/${profileId}`, function(res) {
            if(res.success && res.profile) {
                const profile = res.profile;
                
                let detailHtml = `
                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-primary">Informasi Dasar</h6>
                            <table class="table table-sm">
                                <tr><td><strong>Nama Profile:</strong></td><td><code>${profile.name || '-'}</code></td></tr>
                                <tr><td><strong>Rate Limit:</strong></td><td><code class="text-success">${profile['rate-limit'] || '-'}</code></td></tr>
                                <tr><td><strong>Local Address:</strong></td><td><code>${profile['local-address'] || '-'}</code></td></tr>
                                <tr><td><strong>Remote Address:</strong></td><td><code>${profile['remote-address'] || '-'}</code></td></tr>
                                <tr><td><strong>DNS Server:</strong></td><td><code>${profile['dns-server'] || '-'}</code></td></tr>
                                <tr><td><strong>Parent Queue:</strong></td><td><code>${profile['parent-queue'] || '-'}</code></td></tr>
                                <tr><td><strong>Address List:</strong></td><td><code>${profile['address-list'] || '-'}</code></td></tr>
                                <tr><td><strong>Komentar:</strong></td><td><small class="text-muted">${profile.comment || '-'}</small></td></tr>
                            </table>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-info">Konfigurasi Lanjutan</h6>
                            <table class="table table-sm">
                                <tr><td><strong>Bridge Learning:</strong></td><td><code>${profile['bridge-learning'] || 'default'}</code></td></tr>
                                <tr><td><strong>Use MPLS:</strong></td><td><code>${profile['use-mpls'] || 'default'}</code></td></tr>
                                <tr><td><strong>Use Compression:</strong></td><td><code>${profile['use-compression'] || 'default'}</code></td></tr>
                                <tr><td><strong>Use Encryption:</strong></td><td><code>${profile['use-encryption'] || 'default'}</code></td></tr>
                                <tr><td><strong>Only One:</strong></td><td><code>${profile['only-one'] || 'default'}</code></td></tr>
                                <tr><td><strong>Change TCP MSS:</strong></td><td><code>${profile['change-tcp-mss'] || 'default'}</code></td></tr>
                                <tr><td><strong>Use UPnP:</strong></td><td><code>${profile['use-upnp'] || 'default'}</code></td></tr>
                                <tr><td><strong>Default:</strong></td><td><span class="badge ${profile.default === 'true' ? 'bg-success' : 'bg-secondary'}">${profile.default === 'true' ? 'Ya' : 'Tidak'}</span></td></tr>
                            </table>
                        </div>
                    </div>
                `;
                
                // Tambahkan script on-up dan on-down jika ada
                if(profile['on-up'] || profile['on-down']) {
                    detailHtml += `
                        <div class="row mt-3">
                            <div class="col-12">
                                <h6 class="text-warning">Script Konfigurasi</h6>
                                <div class="row">
                                    ${profile['on-up'] ? `
                                    <div class="col-md-6">
                                        <strong>On-Up Script:</strong>
                                        <pre class="bg-light p-2 rounded" style="font-size: 0.8rem; max-height: 200px; overflow-y: auto;">${profile['on-up']}</pre>
                                    </div>
                                    ` : ''}
                                    ${profile['on-down'] ? `
                                    <div class="col-md-6">
                                        <strong>On-Down Script:</strong>
                                        <pre class="bg-light p-2 rounded" style="font-size: 0.8rem; max-height: 200px; overflow-y: auto;">${profile['on-down']}</pre>
                                    </div>
                                    ` : ''}
                                </div>
                            </div>
                        </div>
                    `;
                }
                
                $('#detailModalLabel').text(`Detail Profile: ${profileName}`);
                $('#profileDetailContent').html(detailHtml);
            } else {
                $('#detailModalLabel').text('Error');
                $('#profileDetailContent').html(`<div class="alert alert-danger">Gagal mengambil detail profile: ${res.message || 'Unknown error'}</div>`);
            }
        }).fail(function() {
            $('#detailModalLabel').text('Error');
            $('#profileDetailContent').html('<div class="alert alert-danger">Gagal menghubungi server</div>');
        });
    });

    // Edit Profile
    $(document).on('click', '.edit-profile-btn', function() {
        const remoteAddressValue = $(this).data('remote-address') || '';
        const button = $(this);
        
        // Load IP pools dengan callback untuk set nilai setelah dimuat
        loadIPPools(function() {
            $('#profileId').val(button.data('id'));
            $('#routerId').val(button.data('router-id') || '');
            $('#profileName').val(button.data('name'));
            $('#rateLimit').val(button.data('rate-limit') || '');
            $('#localAddress').val(button.data('local-address') || '');
            $('#remoteAddress').val(remoteAddressValue);
            $('#dnsServer').val(button.data('dns-server') || '');
            $('#parentQueue').val(button.data('parent-queue') || '');
            $('#addressList').val(button.data('address-list') || '');
            $('#profileComment').val(button.data('comment') || '');
            $('#bridgeLearning').val(button.data('bridge-learning') || 'default');
            $('#useMpls').val(button.data('use-mpls') || 'default');
            $('#useCompression').val(button.data('use-compression') || 'default');
            $('#useEncryption').val(button.data('use-encryption') || 'default');
            $('#onlyOne').val(button.data('only-one') || 'default');
            $('#changeTcpMss').val(button.data('change-tcp-mss') || 'default');
            $('#profileModalLabel').text('Edit Profile PPPoE');
        });
        
        $('#profileModal').modal('show');
    });

    // Submit Tambah/Edit Profile
    $('#profileForm').submit(function(e) {
        e.preventDefault();
        const routerId = $('#routerId').val();
        if (!routerId) {
            showNotification('warning', 'Peringatan!', 'Pilih NAS (router) terlebih dahulu!', 'OK');
            return;
        }
        
        // Bersihkan nilai sebelum dikirim
        const cleanValue = function(val) {
            if (val === null || val === undefined) return '';
            const str = String(val).trim();
            return str === '' ? '' : str;
        };
        
        const formData = {
            id: $('#profileId').val(),
            router_id: routerId,
            name: cleanValue($('#profileName').val()),
            'bridge-learning': $('#bridgeLearning').val(),
            'use-mpls': $('#useMpls').val(),
            'use-compression': $('#useCompression').val(),
            'use-encryption': $('#useEncryption').val(),
            'only-one': $('#onlyOne').val(),
            'change-tcp-mss': $('#changeTcpMss').val()
        };
        
        // Hanya tambahkan field opsional jika tidak kosong
        const rateLimit = cleanValue($('#rateLimit').val());
        const localAddress = cleanValue($('#localAddress').val());
        const remoteAddress = cleanValue($('#remoteAddress').val());
        const dnsServer = cleanValue($('#dnsServer').val());
        const parentQueue = cleanValue($('#parentQueue').val());
        const addressList = cleanValue($('#addressList').val());
        const comment = cleanValue($('#profileComment').val());
        
        if (rateLimit !== '') formData['rate-limit'] = rateLimit;
        if (localAddress !== '') formData['local-address'] = localAddress;
        if (remoteAddress !== '') formData['remote-address'] = remoteAddress;
        if (dnsServer !== '') formData['dns-server'] = dnsServer;
        if (parentQueue !== '') formData['parent-queue'] = parentQueue;
        if (addressList !== '') formData['address-list'] = addressList;
        if (comment !== '') formData.comment = comment;
        
        const url = formData.id ? '/admin/mikrotik/edit-profile' : '/admin/mikrotik/add-profile';
        
        $.post(url, formData, function(res) {
            if(res.success) {
                showNotification('success', 'Success!', 'Profile berhasil disimpan!', 'Continue', function() {
                    location.reload();
                });
            } else {
                showNotification('error', 'Error!', 'Gagal menyimpan profile: ' + (res.message || 'Unknown error'), 'Try again');
            }
        });
    });

     // Hapus Profile
     $(document).on('click', '.delete-profile-btn', function() {
         const name = $(this).data('name');
         const routerId = $(this).data('router-id');
         const id = $(this).data('id');
         const button = $(this);
         
         showConfirmNotification('warning', 'Konfirmasi!', 'Yakin ingin menghapus profile PPPoE "' + name + '"?', 'Hapus', 'Batal', function() {
             $.post('/admin/mikrotik/delete-profile', { id, router_id: routerId }, function(res) {
                 if(res.success) {
                     showNotification('success', 'Success!', 'Profile berhasil dihapus!', 'Continue', function() {
                         location.reload();
                     });
                 } else {
                     showNotification('error', 'Error!', 'Gagal menghapus profile: ' + (res.message || 'Unknown error'), 'Try again');
                 }
             });
         });
     });

     // Refresh Data
     $('#btnRefresh').on('click', function() {
         location.reload();
     });

     // Filter dan Pencarian
     $('#searchInput').on('keyup', function() {
         profileTable.search($(this).val()).draw();
     });

     $('#statusFilter').on('change', function() {
         const status = $(this).val();
         $.fn.dataTable.ext.search.pop(); // Hapus filter sebelumnya
         
         if (status) {
             $.fn.dataTable.ext.search.push(function(settings, data, dataIndex) {
                 const statusText = data[6]; // Kolom Status
                 return statusText.includes(status === 'default' ? 'Default' : 'Custom');
             });
         }
         profileTable.draw();
     });

     $('#rateFilter').on('change', function() {
         const rateFilter = $(this).val();
         $.fn.dataTable.ext.search.pop(); // Hapus filter sebelumnya
         
         if (rateFilter) {
             $.fn.dataTable.ext.search.push(function(settings, data, dataIndex) {
                 const rateText = data[2]; // Kolom Rate Limit
                 if (rateFilter === 'has-rate') {
                     return rateText !== '-' && rateText.trim() !== '';
                 } else if (rateFilter === 'no-rate') {
                     return rateText === '-' || rateText.trim() === '';
                 }
                 return true;
             });
         }
         profileTable.draw();
     });

     $(document).on('click', '#restartMikrotikBtn', function(e) {
        e.preventDefault();
        $('#restartMikrotikModal').modal('show');
     });
     $('#confirmRestartMikrotik').on('click', function() {
        $('#restartMikrotikModal').modal('hide');
        $.ajax({
          url: '/admin/mikrotik/restart',
          method: 'POST',
          success: function(res) {
            let notif = $('#restartNotif');
            if(res.success) {
              notif.removeClass('d-none alert-danger').addClass('alert-success').text(res.message || 'Mikrotik berhasil direstart!');
            } else {
              notif.removeClass('d-none alert-success').addClass('alert-danger').text(res.message || 'Gagal restart Mikrotik!');
            }
            setTimeout(function() { notif.addClass('d-none'); }, 4000);
          },
          error: function() {
            let notif = $('#restartNotif');
            notif.removeClass('d-none alert-success').addClass('alert-danger').text('Gagal menghubungi server!');
            setTimeout(function() { notif.addClass('d-none'); }, 4000);
          }
        });
     });
 });
 </script>

 <script>
 // Fungsi untuk set preset rate limit
 function setRateLimit(value) {
     document.getElementById('rateLimit').value = value;
 }
 
 // Fungsi untuk set preset local address
 function setLocalAddress(value) {
     document.getElementById('localAddress').value = value;
 }

 // Fungsi untuk set preset remote address

 // Fungsi untuk set preset DNS server
 function setDnsServer(value) {
     document.getElementById('dnsServer').value = value;
 }
 
 // Hamburger & sidebar logic
 const hamburger = document.getElementById('hamburgerBtn');
 const sidebar = document.getElementById('adminSidebar');
 const overlay = document.getElementById('sidebarOverlay');
 if (hamburger && sidebar && overlay) {
     hamburger.addEventListener('click', function() {
         sidebar.classList.toggle('show');
         overlay.classList.toggle('show');
     });
     overlay.addEventListener('click', function() {
         sidebar.classList.remove('show');
         overlay.classList.remove('show');
     });
 }
 </script>

 <%- include('partials/footer') %>
 </body>
 </html>